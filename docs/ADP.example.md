# ADP Example (Narrative)

An end‑to‑end narrative that uses the contract layer with the concrete implementations shipped in `src/`
(FlightsRepositoryFS, WeatherProviderMeteostat, ADPFeatureBuilder, ADPModelService).